# Ruakdown 使用指南

> 面向使用者的操作手册。架构决策与实现细节见 [DESIGN.md](DESIGN.md),各版本升级差异见 [CHANGELOG.md](CHANGELOG.md)。
> 本指南对应当前版本 **v0.6.0**。

## 目录

- [一、这是什么](#一这是什么)
- [二、安装与启动](#二安装与启动)
- [三、界面速览](#三界面速览)
- [四、打开与浏览文档](#四打开与浏览文档)
- [五、编辑与保存](#五编辑与保存)
- [六、分屏模式(源码 + 实时预览)](#六分屏模式源码--实时预览)
- [七、专注模式(Zen)](#七专注模式zen)
- [八、目录内搜索](#八目录内搜索)
- [九、主题、背景图与设置](#九主题背景图与设置)
- [十、导出离线 HTML](#十导出离线-html)
- [十一、预览服务与局域网分享](#十一预览服务与局域网分享)
- [十二、快捷键总表](#十二快捷键总表)
- [十三、配置文件与数据位置](#十三配置文件与数据位置)
- [十四、已知限制与常见问题](#十四已知限制与常见问题)
- [十五、从源码构建(开发者)](#十五从源码构建开发者)

---

## 一、这是什么

Ruakdown 是一个 **Windows 优先、离线优先的本地 Markdown 阅读器 / 编辑器**,基于 Tauri 2(Rust)+ React 19 + TypeScript 构建。它不联网、不绑定云服务:打开本地文件夹或文件,渲染、编辑、导出、分享全部在本机完成。

与常见 Markdown 编辑器相比,它的几个取舍点:

- **阅读优先**:三档视图「阅读 / 分屏 / 源码」,默认进阅读。渲染由 Rust 侧 `pulldown-cmark` 完成,阅读视图、导出 HTML、局域网分享页共用同一条渲染管线,三处结果必然一致。
- **大文档可读**:超过 1MB 的文档自动切到分块虚拟化渲染(按需挂载 + DOM 驱逐),几十 MB 的日志式文档也能流畅滚动。
- **编码与换行不破坏**:逐文件保持原编码(UTF-8 / BOM / UTF-16 / GBK)与换行风格(CRLF / LF),不改写你没动过的部分。
- **可分享**:内置 axum 预览服务,支持本机预览与局域网三档权限分享(只读 / 同步浏览 / 协作编辑),带访问令牌鉴权,分享页离线可用。

功能一览:

| 类别 | 能力 |
|------|------|
| 阅读 | 文件树、大纲导航与滚动高亮、Mermaid 图表、代码语法高亮、`==高亮==`、图片悬停放大与灯箱、大文档分块渲染 |
| 编辑 | CodeMirror 6 源码模式、自动保存、编码/换行保持、保存前滚动备份、外部修改检测 |
| 分屏 | 左右并排、比例拖拽、双向滚动同步、实时预览 |
| 专注 | 按标题分节聚焦、章节跳转、重点增强 |
| 检索 | 目录内全文搜索(内容 + 文件名),结果跳转定位 |
| 输出 | 离线单文件 HTML 导出、本机预览服务、局域网分享 |
| 外观 | 10 款内置主题、阅读区背景图(实色 / 毛玻璃) |
| 平台 | `.md` 文件关联、单实例、双击打开、会话恢复 |

---

## 二、安装与启动

### 下载安装包

从 GitHub Releases 下载对应平台安装包:

| 平台 | 文件 | 说明 |
|------|------|------|
| Windows x64 | `Ruakdown_<版本>_x64-setup.exe` | NSIS 安装器,按当前用户安装,无需管理员 |
| macOS (Apple Silicon) | `Ruakdown_<版本>_aarch64.dmg` | 另有 `.app.tar.gz`;自 v0.3.0 起提供 |

Intel Mac 与 Linux 暂无预编译包,需自行从源码构建(见[第十五节](#十五从源码构建开发者))。

### 首次启动

- **Windows**:安装器会自动关联 `.md` / `.markdown`,之后在资源管理器里双击文档即可用 Ruakdown 打开。
- **macOS**:未做代码签名与公证,首次打开若提示「来源不明」,到「系统设置 → 隐私与安全性」中允许即可。同样支持 Finder「打开方式」与拖到 Dock 图标打开。

启动后会**恢复上次会话**(上次打开的文件夹、文件、主题与各项设置)。如果启动时带了要打开的文件(双击 `.md`),该文件优先于会话恢复。

**单实例**:Ruakdown 只允许运行一个实例;重复启动(或双击另一个 `.md`)会唤起已有窗口并切换到目标文件,不会开第二个窗口。

---

## 三、界面速览

```
┌──────────────────────────────────────────────────────────────┐
│ 标题栏  [阅读|分屏|源码]  打开文件夹 打开文件 搜索 专注 主题▾  │
│          设置  全屏                     (仅在有文档时出现)     │
├──────────────┬───────────────────────────────────────────────┤
│ 侧边栏       │  内容区                                       │
│ [文件树|大纲]│  阅读视图 / 分屏(编辑器+预览)/ 源码编辑器      │
│              │                                               │
├──────────────┴───────────────────────────────────────────────┤
│ 状态栏  文件名 · 视图 · 编码 · 换行 · 未保存 · [分享信息]      │
└──────────────────────────────────────────────────────────────┘
```

- **标题栏**:视图三态切换、打开入口、搜索、专注、主题下拉、设置、全屏。
- **侧边栏**:两个标签页 —— **文件树**(当前文件夹的目录结构)与**大纲**(当前文档的标题树)。可通过「视图 → 显示/隐藏侧边栏」收起;全屏时侧边栏与状态栏一并隐藏。
- **状态栏**:当前文件名、视图模式、专注进度、文件编码与换行风格、未保存标记;分享服务运行时额外显示分享档位、链接与「跟随远端」开关。

---

## 四、打开与浏览文档

### 打开文件夹 / 打开文件

两种方式(Ctrl/Cmd+Shift+O 打开文件夹,Ctrl/Cmd+O 打开文件,或标题栏按钮):

- **打开文件夹**:左侧出现文件树,可点击任意 `.md` 阅读;此模式下「目录内搜索」可用。文件树只收录 `.md` / `.markdown`,**跳过**隐藏目录(以 `.` 开头)与 `.git`、`node_modules`、`target`、`dist` 等目录,递归深度上限 16 层。目录内文件增删改会自动刷新(基于文件系统监听,事件防抖合并)。
- **打开文件**:只打开单个文档,不建立文件树。

### 阅读辅助

- **frontmatter 属性卡**:文档顶部由 `---` 包围的元数据块(如 `title:`、`tags:`)渲染为正文开头的声明式键值卡片——键名灰色小字、值正常字色,列表值折叠为一行,不再显示成分隔线加 setext 大标题,也不会混进大纲。阅读、分屏预览、大文档、导出 HTML 与分享页表现一致。
- **大纲**:侧边栏「大纲」页列出 H1–H6 标题,点击跳转,滚动时自动高亮当前章节。
- **Mermaid**:` ```mermaid ` 代码块按需渲染为 SVG(文档里没有图表就不加载渲染引擎),图表配色随亮/暗主题同步。
- **代码高亮**:围栏代码块按语言做语法高亮,覆盖预览、分屏预览、阅读视图、分享页与 HTML 导出。未知语言与 mermaid 块按纯文本显示。
- **`==高亮==`**:文中用 `==文字==` 可标记荧光笔重点,跳过代码块,所有视图可用。
- **图片**:所有视图统一限高 60vh,超出等比缩小;**鼠标悬停**在原位置浮出原始尺寸预览,**单击**打开居中灯箱(Esc 或点击关闭)。两种浮层都不占布局,不会扰动滚动位置。
- **链接**:点击正文里的链接 —— 站内或本地 `.md` 在应用内打开(侧栏文件树保持不变);`http(s)` / `mailto` 交给系统浏览器/邮件;`#锚点` 在文内平滑滚动;其他本地文件用系统默认程序打开。源码模式下 `Ctrl/Cmd+点击` 链接同样生效。

### 大文档

源文件超过 **1MB** 自动进入分块渲染:按 Markdown 顶层块切分(不会切开表格/列表/代码块),视口附近按需挂载,滚远的块从 DOM 中驱逐但保留实测高度,配合滚动锚定补偿,快速滚动不会白屏也不会跳动。分块大文档下专注模式不可用(入口隐藏),浏览器内 Ctrl+F 也主要搜索已挂载的块 —— 全文检索请用「目录内搜索」。

---

## 五、编辑与保存

切到「源码」视图即为 CodeMirror 6 编辑器,Markdown 语法高亮、撤销/重做、查找等编辑能力齐备。

- **自动保存**:默认开启,停止输入 1.5 秒后写盘。可在「设置 → 自动保存」关闭。
- **手动保存**:Ctrl/Cmd+S。
- **编码与换行保持**:打开时探测文件编码(UTF-8 有/无 BOM、UTF-16、GBK 回退)与换行风格(CRLF / LF),保存时按原样写回;新建文件默认 UTF-8 无 BOM。
- **保存前备份**:每次写盘前把当前内容备份到配置目录的 `backups/` 下(命名 `<文件名>.<时间戳>.bak`),每个文件滚动保留最近 10 份。注意:滚动备份跟随当前版本,同一文件在配置目录下只保留最新 10 份。
- **外部修改检测**:文件被其他程序改动时,顶部滑下提示条「文件已被外部修改」,可选择「重新加载」或「忽略」。为避免保存后误报,应用自身写盘后的 **3 秒内**不做外部变更检查 —— 代价是他人恰好在这 3 秒内改了同一文件,该次改动不会立即提示(继续编辑或重新打开后仍会提示)。

提示条与错误横幅都是浮层,出现与消失不会让正文位移。

---

## 六、分屏模式(源码 + 实时预览)

标题栏点「分屏」或按 **Ctrl+Tab** 循环切换视图(阅读 → 分屏 → 源码 → 阅读)。

- **布局**:左编辑器右预览,可**拖拽中间分隔条**调整比例(20%–80%);悬停出现的 **⇄** 按钮互换左右;在分隔条上**双击**恢复 50/50。方向与比例写入配置,下次启动保持。
- **滚动同步**:块级双向联动,以编辑器为准。滚动编辑器 → 预览跟随;滚动预览 → 编辑器跟随(150ms 同步锁防回环,块内不追像素)。
- **大纲联动**:分屏下大纲取自实时预览,点击大纲跳到编辑器对应行;目录搜索命中也会跳到编辑器行号;当前章节高亮由编辑器顶行驱动。
- **预览刷新**:编辑输入停止 280ms 后重建预览,未改动的块按内容哈希复用渲染结果,过期响应直接丢弃。上方内容增删时预览会重新对齐当前视口,不会跳动。
- **切换视图时的编辑缓冲**:分屏 ↔ 源码 互切保留未保存内容;从分屏回阅读视图会**先保存**。

---

## 七、专注模式(Zen)

只保留当前章节清晰、其余调暗(可选再加模糊),适合逐节精读。入口:标题栏「专注」按钮、视图菜单「专注模式」或 **Ctrl/Cmd+Shift+Z**。

- **分节级别**:自动(取出现 ≥2 次的最上层标题)/ 强制 H1 / H2 / H3,在设置里指定。
- **效果强度**:仅调暗 / 调暗 + 模糊。
- **重点增强**:开启后,当前章节内的**粗体**微放大 + 荧光笔色带、`行内代码`加光晕、引用块 callout 化。
- **章节导航**:`←` / `→` 或 `j` / `k` 跳转上一节/下一节,焦点跟随滚动切换,状态栏显示「第 n / 总数 节」。
- **退出**:Esc(退出顺序:先关搜索/设置,再退专注,最后退全屏)。

限制:分块渲染的大文档(>1MB)不支持专注模式,按快捷键/菜单会给出提示横幅;分屏或源码视图下按 `Ctrl+Shift+Z` 会先落回阅读视图并**直接开启**专注(再次按键才是关闭);切换离开阅读视图时专注自动退出,不会残留隐藏的开启状态。

---

## 八、目录内搜索

**Ctrl/Cmd+Shift+F** 或标题栏「搜索」按钮打开搜索框(需先打开文件夹)。

- 递归搜索当前文件夹下所有 `.md` / `.markdown` 的**正文内容**与**文件名**,文件名命中排在最前;文件名匹配与 GBK / UTF-16 编码文档均可搜到(复用与阅读一致的编码探测)。
- 关键词至少 **2 个字符**才开始搜;输入防抖 500ms;可切「Aa」大小写敏感。
- 结果按文件分组,展示行号 + 命中行片段(关键词高亮),点击即打开文件并定位:阅读视图会滚动到首个匹配处;分屏视图跳到编辑器对应行;分块大文档与源码模式为 best-effort(仅打开)。
- 键盘:↑/↓ 选择,Enter 打开,Esc 关闭。
- 上限保护:单文件最多 50 条命中,总量 2000 条后截断(状态行提示),超过 16MB 的文件跳过;列表最多渲染 300 条,但计数显示真实值。

---

## 九、主题、背景图与设置

### 主题

内置 **10 款**主题,标题栏下拉或「主题」菜单切换,立即生效、无需刷新,并同步窗口标题栏深浅色与 Mermaid 配色:

| 分组 | 主题 |
|------|------|
| 基础 | 浅色、暗色、石墨 |
| 自然/生活 | 夕阳海岸、无边绿意、蓝天白云、陈旧报纸 |
| 中式 | 青梅煮酒、高山流水、论道武当(黑白织染风格) |

阅读区背景图(设置里配置)会把主题作为前景色,叠加在图片之上:

- **纸面实色**:背景图上盖一层不透明纸面,图片作为纹理隐约可见;
- **半透明毛玻璃**:图片模糊后作为底,可调模糊度(0–40px)与蒙版浓度(0–100%);
- 蒙版颜色随主题自动适配(浅色主题叠白纱、深色主题叠暗纱);背景图只作用于阅读区。

### 设置面板

| 设置项 | 说明 |
|--------|------|
| 自动保存 | 输入暂停 1.5 秒后写盘 |
| 预览服务端口 | 分享/预览服务监听端口(1024–65535);**下次启动服务时生效** |
| 阅读区背景图 | 启用开关、图片选择、样式、模糊度、蒙版浓度 |
| 专注模式 | 分节级别、效果强度、重点增强 |

---

## 十、导出离线 HTML

**Ctrl/Cmd+E**(或「文件 → 导出 HTML...」),选择保存路径(默认文件名 `<文档名>.html`)后生成**单文件** HTML:

- 与阅读视图同一条 Rust 渲染管线,主题 CSS 变量内联,代码高亮样式一致;
- Mermaid 渲染脚本默认**内嵌**(断网也能打开图表);资源缺失时回退 CDN;
- **图片仍为相对引用**:把 HTML 导出到与原文档同一目录(或保持相同的相对路径结构),图片照常显示;图片本身不会被打包进 HTML。

---

## 十一、预览服务与局域网分享

菜单「服务」下有四个入口:

| 菜单项 | 监听地址 | 权限 |
|--------|----------|------|
| 本机预览服务 | `127.0.0.1` | 仅本机浏览器访问,只读 |
| 局域网分享(只读) | `0.0.0.0` | 局域网内可看,只读 |
| 局域网分享(同步浏览) | `0.0.0.0` | 远端滚动 ↔ 本机双向同步 |
| 局域网分享(协作编辑) | `0.0.0.0` | 远端可提交修改,本机落盘 |

另有「停止分享服务」与「在浏览器打开分享页」。服务运行时状态栏会显示当前档位、分享链接与「复制链接」按钮。

### 访问链接与令牌

- 分享地址形如 `http://192.168.x.x:端口/?t=<令牌>`。地址使用**本机网卡真实 IP**(不再输出 `0.0.0.0`),可直接发给同事打开。
- 每次启动分享服务都会重新生成 **128 位随机访问令牌**,页面通过 `?t=` 换取 cookie,之后全路由(含 WebSocket 握手)鉴权。**切换档位会重启服务并换新令牌,旧链接立即失效**;停止再开也一样,需重新复制链接。
- 端口被占用时自动向后尝试 **+1 ~ +10**,状态栏回显实际绑定端口。
- Windows 上首次以 `0.0.0.0` 监听会触发防火墙授权弹窗,需允许才能被局域网访问。

### 远端分享页看到什么

- 与阅读视图一致的渲染结果(含语法高亮内的代码块),**跟随系统暗色**自动切换深浅配色;
- 右上角「目录」按钮展开文档大纲,点击跳转;
- Mermaid 使用应用内置的渲染资源,**离线可用**(内嵌资源缺失时才回退 CDN);
- 顶部状态条显示文档标题、连接状态、更新时间,以及「本机有未保存修改」提示;
- 图片可正常显示(相对图片路径已改写)。

### 三档权限的具体行为

- **只读**:远端仅浏览,不能滚动同步也不能编辑。
- **同步浏览**:远端滚动会推送给本机;本机默认**不**跟随,需在状态栏显式打开「跟随远端:开」后才跟随(双向:本机滚动也会推给远端,远端自动跟随)。这样分享者不会被他人的滚动抢走视线。
- **协作编辑**:远端分享页出现「编辑」按钮,提交后由本机写入并落盘;若**本机当前有未保存改动**,会拒绝远端写入并提示,避免覆盖。本机保存后,远端通过 WebSocket 推送 + ETag 协商自动刷新。

### 分享实现要点(排障相关)

- 刷新链路为 WebSocket 推送 + `/api/doc` 的 ETag/304 协商 + 单槽渲染缓存,无轮询;静态资源启用 ETag 缓存。
- 安全上已移除宽松 CORS(任意网页不能跨域读文档),`/static` 目录带 CSP sandbox 与 `nosniff`,保留目录穿越 403。

---

## 十二、快捷键总表

macOS 上 `Ctrl` 对应 `⌘`(全屏与 `Ctrl+Tab` 除外)。

| 快捷键 | 作用 |
|--------|------|
| `Ctrl/Cmd+Shift+O` | 打开文件夹 |
| `Ctrl/Cmd+O` | 打开文件 |
| `Ctrl/Cmd+S` | 保存 |
| `Ctrl/Cmd+E` | 导出 HTML |
| `Ctrl/Cmd+Shift+F` | 目录内搜索 |
| `Ctrl/Cmd+Shift+Z` | 专注模式开/关 |
| `Ctrl+Tab` | 视图循环:阅读 → 分屏 → 源码 |
| `F11`(Windows)/ `⌃⌘F`(macOS) | 全屏(隐藏标题栏、侧栏、状态栏) |
| `Esc` | 逐层退出:搜索/设置 → 专注 → 全屏 |
| `←` / `→`、`j` / `k` | 专注模式下上一节 / 下一节 |
| `Ctrl/Cmd+点击`(源码模式) | 打开光标所在链接 |

注意:`Ctrl+Tab` 在 v0.4.0 起是**三态循环**(旧版是阅读/源码两态切换);`Ctrl+Shift+Z` 被应用截获用于专注模式,不会触发编辑器「重做」。应用内没有实现文档内查找,Ctrl+F 交给 WebView 自带查找(仅 Windows WebView2 提供;且只搜到已挂载的内容,全文检索请用目录内搜索)。

---

## 十三、配置文件与数据位置

| 内容 | 位置 |
|------|------|
| 配置文件 | Windows `%APPDATA%\com.ruakdown.app\config.json`;macOS `~/Library/Application Support/com.ruakdown.app/config.json` |
| 保存前备份 | 同目录下 `backups/<文件名>.<时间戳>.bak`,每文件保留最近 10 份 |

`config.json` 记录的字段(全部可选,缺失即用默认值,新增字段向后兼容追加):

| 字段 | 含义 |
|------|------|
| `lastFolder` / `lastFile` | 会话恢复用 |
| `theme` | 当前主题 id |
| `servePort` | 预览/分享服务端口 |
| `autosave` | 自动保存开关 |
| `background` | 阅读区背景图(启用、路径、样式、模糊度、蒙版浓度) |
| `zen` | 专注模式(分节级别、效果、重点增强) |
| `split` | 分屏(编辑器在左/右、比例) |

升级安装不会清除配置:下载新版本安装包覆盖安装即可,主题、布局、会话等设置保留。

---

## 十四、已知限制与常见问题

**分享相关**

- 升级到 v0.6.0 后,**旧版分享出去的链接会鉴权失败**(旧链接不含令牌),重新生成即可。旧版显示的局域网地址本就不可用(`0.0.0.0`),新版展示的才是能直接打开的地址。
- 让远端看到**最新内容**需要本机已保存(自动保存开启时,停手 1.5 秒即落盘)。
- 协作编辑时本机有未保存改动会拒绝远端写入,先把本机内容保存或处理掉。

**阅读/渲染**

- 大文档(>1MB)不支持专注模式;WebView 自带查找(Windows 下 Ctrl+F)只搜已挂载的块,全文检索请用目录内搜索。
- 超大文档(数千标题)大纲列表未做虚拟化,可能略卡;常驻内存约为文件体积的 2 倍。
- 原始 HTML 在文档中是透传渲染的(信任本地文档),不要打开来源不可信的 Markdown。
- 外链图片、尺寸探测失败的图片在分屏下依赖 `ResizeObserver` 自愈锚定。

**编辑**

- 保存后 3 秒内不检测外部修改(见第五节),有极小概率漏报一次他人改动。
- 导出的 HTML 不内嵌图片,只保留相对引用。

**平台**

- 预编译包只有 Windows x64 与 macOS arm64;Intel Mac / Linux 需自行构建。
- macOS 未签名/公证,首次打开需在隐私与安全性里放行。
- 16 线程并行链接可能触发 `LNK1102 内存不足`,构建时已固定 `CARGO_BUILD_JOBS=4`。

---

## 十五、从源码构建(开发者)

### 前置依赖

Node + pnpm、Rust(Windows 用 MSVC 工具链)、WebView2(Win11 自带)、[go-task](https://taskfile.dev)。

```sh
task doctor     # 检查 node/pnpm/rustc/cargo 与产物目录
task install    # pnpm install
task dev        # 开发模式(tauri dev,Vite HMR + Rust 增量编译)
```

### 构建与测试

| 命令 | 作用 |
|------|------|
| `task check` | 快速检查:前端 `tsc + vite build` + Rust `cargo check` |
| `task test` | Rust 单元测试 |
| `task bench` | 大文档渲染基准(1/5/10MB,release,ignored test) |
| `task build` | release + 平台安装包(win→NSIS / mac→dmg / linux→deb) |
| `task build:frontend` | 仅构建前端(`dist/`) |
| `task clean` | 清理 `dist/` 与 cargo target |

前端另有 vitest 测试(`pnpm test`,覆盖链接路由、文件变更决策、滚动同步、分屏纯函数)。

安装包产物在 `${CARGO_TARGET_DIR:-src-tauri/target}/release/bundle/` 下。`task build` 与 `tauri dev` 共用 target 目录,**不要同时运行**。

### 发布流程

1. 在 [CHANGELOG.md](CHANGELOG.md) 顶部新增 `## [x.y.z] - YYYY-MM-DD` 段落,写清相对上一版本的差异;
2. 同步 `src-tauri/tauri.conf.json`、`src-tauri/Cargo.toml`、`package.json` 的版本号;
3. 提交后推送 `v*` tag,`.github/workflows/release.yml` 自动构建 Windows / macOS 安装包,并从 CHANGELOG 对应段落提取 Release 说明。

### 项目结构速查

```
src/                     前端(React + TS)
├── App.tsx              应用外壳、三态视图、菜单/快捷键路由、设置弹窗
├── components/          FileTree / Outline / Reader / ChunkedReader /
│                        SourceEditor / PreviewPane / SplitView / SearchModal
├── links.ts             Markdown 链接路由(外链 / 锚点 / 本地 md)
├── split.ts            三态循环与分屏比例纯函数
├── scrollSync.ts       编辑器↔预览块级滚动同步
├── zen.ts              专注模式分节与 ==高亮== 处理
└── imgZoom.ts          图片悬停放大与灯箱

src-tauri/src/          Rust 侧
├── commands.rs          Tauri command 桥(薄层)
├── lib.rs               菜单、状态、单实例、启动参数
└── core/                无 tauri 依赖的纯业务逻辑
    ├── markdown.rs      pulldown-cmark 渲染 + 语法高亮 + 大纲提取
    ├── large_doc.rs     分块渲染与块索引
    ├── preview.rs       分屏预览缓存(增量块复用)
    ├── serve.rs         axum 分享服务(令牌鉴权 / WS / 静态资源)
    ├── file.rs          目录树、编码探测、读写、备份
    ├── search.rs        目录内搜索
    ├── export.rs        离线 HTML 导出
    ├── theme.rs         内置主题(include_str! 内嵌)
    ├── link.rs          链接解析
    └── watch.rs         文件系统监听
```

设计原则:`core/` 不依赖 tauri(可独立编译为 lib);`commands.rs` 只做参数转换与桥接;前端不引入 UI 组件库,样式走 Tailwind + CSS 变量;渲染事实源唯一(Rust),前端只负责 Mermaid 的按需渲染。
