# 更新日志

Ruakdown 各版本**相对上一版本的升级差异**,按时间倒序排列(最新在最上方)。

发版流程:在文件顶部(最新版本段落之上)新增 `## [x.y.z] - YYYY-MM-DD` 段落,写清相对上一版本的差异;
同步 `src-tauri/tauri.conf.json`、`src-tauri/Cargo.toml` 与 `package.json` 的版本号,提交后推送 `v*` tag。
Release 说明由 `.github/workflows/release.yml` 自动从本文件对应版本段落提取,无需另写。

## 安装包与升级方式

| 平台 | 文件 |
|------|------|
| Windows x64 | `Ruakdown_<版本>_x64-setup.exe`(NSIS,按当前用户安装,无需管理员) |
| macOS (Apple Silicon) | `Ruakdown_<版本>_aarch64.dmg`,另有 `Ruakdown_aarch64.app.tar.gz`;自 v0.3.0 起提供 |

- 平台支持:Windows x64 与 macOS arm64(Apple Silicon);Intel Mac、Linux 暂无预编译包。
- 升级方式:下载新版安装包覆盖安装即可。配置(`config.json`)与主题、布局、会话等设置均保留,新增配置项一律向后兼容追加。
- macOS 首次打开如提示来源不明,在「系统设置 → 隐私与安全性」中允许即可(未做代码签名与公证)。

---

## [0.7.1] - 2026-09-22

相比 v0.7.0:两项体验补全。

### 新增
- **应用内仓库与更新入口**:「帮助」菜单新增「GitHub 仓库」「检查更新 (Releases)...」两项;「设置 → 关于与更新」显示当前版本号并提供同样按钮(未打开文档的首页也增加了入口),一键在浏览器打开;「帮助 → 关于 Ruakdown」对话框同步显示版本号。应用更新仍通过 GitHub Releases 发布,覆盖安装保留全部配置。

### 修复
- **专注模式改为「移动聚光灯」**:聚光灯不再固定在屏幕正中——中段仍点亮聚焦带所在的节,滚到文档顶部/底部时自动移到第一节/最后一节,页面长度保持自然(不追加空白滚动区)。整篇内容不满一屏的短文档,`←`/`→`、`j`/`k` 或滚轮可直接逐节移动聚光灯;`←`/`→`、`j`/`k` 跳转在中段仍将目标节居中到聚焦带,首尾节则停在文档边缘。`↑`/`↓` 保持原生节内细滚不受影响。

---

## [0.7.0] - 2026-09-21

相比 v0.6.2:优化版本——不新增功能,聚焦体积、内存与渲染热路径。

### 变更
- **局域网分享改为可选编译**:axum / tokio 移至 `share` cargo feature,默认构建为不含分享的轻量版(无「服务」菜单,相关命令不注册,前端零改动);`task dev:share` / `task build:share` 编译全量版。CI 发布安装包仍为全量版。
- **mermaid 单份化**:webview 不再打包 npm mermaid,改为按需加载内置的 `resources/mermaid.min.js`(与导出 HTML、分享页同一份),前端产物从 5.9MB 降到 0.8MB,三处渲染从此不存在版本漂移;`@codemirror/search` 未使用依赖一并移除。
- **渲染样式单一来源**:阅读视图 / 导出 HTML / 分享页共用的 markdown 内容样式收敛为 `resources/md-content.css` 一份(Rust `include_str!` + 前端 `@import`),改配色不再需要同步三处;顺带对齐三处的微小视觉差异(分享页表格斑马纹、导出页图片 60vh 上限等)。
- 设计过程产物(图标初稿、对比图、演示页)移出仓库,保留在本地 `design/`(已 gitignore),仓库瘦身约 10MB。

### 性能
- **大文档内存**:移除打开文档时冗余的全文字段克隆;分块缓存加上限(保留最近 3 篇),连续打开多个大文档不再线性占用内存直到退出。
- **分屏预览**:Rust 侧按修订号缓存图片路径重写,同一次编辑往返的反复拉取不再重复重写;前端同一修订号的预览更新跳过整面板卸载重建,消除打字时的预览闪动与请求风暴。
- **滚动热路径**:阅读视图滚动高亮改为 rAF 合帧(原来每个滚动事件全量扫描标题);文件树 / 大纲 / 侧栏全部 memo 化,打字与滚动不再重渲染整棵目录树。
- **文件监听**:`fs-change` 事件 600ms 防抖合并,自动保存不再触发反复的全树重扫。
- **主线程占用**:`render_chunks` / `get_config` / `save_config` / `resolve_link` 移入后台线程;分享服务图片请求的 `canonicalize` 不再阻塞 async worker。
- **细节**:目录搜索每行只做一次大小写折叠;保存时换行转换改为单趟;图片悬停放大 rAF 节流并缓存悬停层尺寸;搜索结果悬停行级 memo(300 行列表不再整表重渲染)。

### 修复
- 目录搜索片段窗口在大小写折叠会缩短字节的字符(如 U+1E9E)上可能越界崩溃的潜在问题。
- 清理死代码:前端从未调用的 `stop_watch` / `list_themes` / `serve_status` 命令及封装、只写不读的缓存字段、十余处过度 pub 导出。

[完整变更](https://github.com/cangyunye/ruakdown/compare/v0.6.2...v0.7.0)

## [0.6.2] - 2026-09-20

### 新增
- **frontmatter 属性卡按键定制**:常见元数据键各有观感,键名匹配不区分大小写——`title` 放大加粗并随主题标题色;`name` / `author` / `version` 加粗;`tags` / `categories` / `keywords` 按逗号、顿号、分号拆为彩色胶囊,同一标签恒定同色(FNV-1a 哈希在 8 色板内选色);`date` 统一矫正为 `yyyy-MM-dd HH:mm:ss`(兼容 `-`/`/`/`.`/中文日期与 ISO `T` 写法,缺时间补 `00:00:00`,丢弃 `Z`/`±hh:mm` 时区尾巴不做换算,无法识别的写法原样显示);`description` 弱化斜体;其余键保持原样。
- **对象数组一行一项**:列表项为 `k: v` 结构时(如 `references:` 下的 `- path: xx` 加缩进 `desc: xx`)渲染为一行一项,键名弱化、同项多字段用 ` · ` 分隔,不再与逗号互相混淆;纯标量列表仍折叠为一行,不限定具体键名。
- **主题胶囊色板**:10 个内置主题各自适配 8 色胶囊色板(「陈旧报纸」「论道武当」用降饱和色系、「石墨」用夜色系,其余复用亮/暗通用板),阅读 / 分屏预览 / 大文档分块 / 导出 HTML / 局域网分享全链路一致;主题 JSON 未配置色板时回退通用亮色板。

[完整变更](https://github.com/cangyunye/ruakdown/compare/v0.6.1...v0.6.2)

## [0.6.1] - 2026-09-19

### 新增
- **frontmatter 属性卡**:文档顶部 `---` 元数据块不再渲染成 `<hr>` + setext 大标题(也不再污染大纲),改为正文顶部的声明式键值卡片(键名灰色小字、值正常字色,列表值折叠为一行)。阅读 / 分屏预览 / 大文档分块 / 导出 HTML / 局域网分享全链路渲染一致。

### 修复
- **专注模式快捷键失效**:根因是 v0.4.0 三态视图后切换视图不重置专注状态——隐藏的「已开启」状态残留时按 `Ctrl+Shift+Z` 只会把它翻转归零,看起来毫无反应。现在:离开阅读视图自动退出专注;非阅读视图按弦总是「落回阅读并开启专注」;大文档按键给出横幅提示而非静默空转;快捷键兜底新增输入法合成(`isComposing`)与按住重复(`repeat`)守卫,搜索/设置弹窗打开时不再误触专注。
- **阅读区空档无法滚动**:正文两侧留白(860px 版心之外)属于 `overflow:hidden` 的包裹层,滚轮落空。现在空档处的滚轮事件转发给正文滚动容器,普通与大文档同样生效。

## [0.6.0] - 2026-09-18

相比 v0.5.1:

### 新增
- **局域网分享三档权限**:只读 / 同步浏览(双向滚动同步,本机显式开启跟随后生效)/ 协作编辑(远端修改回写本机并落盘;本机有未保存改动时拒绝远端写入)。分享页底部显示当前档位、复制链接与「跟随远端」开关,菜单按档位拆分。
- 分享页体验:跟随系统暗色、文档目录面板、mermaid 使用内置资源渲染(离线可用)。

### 变更
- 应用图标更换为「MD 文档 + 眼镜线稿」风格,并重新生成各平台图标(源稿与说明存于 `design/`)。

### 修复
- 分享页相对路径图片裂图:分享渲染复用块级装配器,相对图片改写到 `/static/` 编码路径。
- 局域网分享地址不可用:改用本机网卡真实 IP(不再输出 `0.0.0.0`);端口被占用时自动 +1..+10 回退,并回显实际绑定端口。

### 安全
- 移除宽松 CORS,杜绝任意网页跨域读取文档。
- 每次启动生成 128 位访问令牌,全路由鉴权(`?t=` 换取 cookie,含 WebSocket 握手)。
- `/static` 增加 CSP sandbox 与 `nosniff`,保留目录穿越 403。

### 性能
- 分享服务去掉 3 秒轮询,改为 WebSocket 推送 + `/api/doc` ETag/304 + 单槽渲染缓存;静态文件启用 ETag 协商缓存。

### ⚠️ 升级提示
- 分享链接现在携带访问令牌,**旧版分享出去的链接(无令牌)会鉴权失败**,请升级后重新生成。
- 旧版给出的局域网地址本就不可用(`0.0.0.0`),新版展示的地址才是可直接打开的。

[完整变更](https://github.com/cangyunye/ruakdown/compare/v0.5.1...v0.6.0)

## [0.5.1] - 2026-09-16

相比 v0.5.0:

### 新增
- 图片悬停放大浮层(跟随鼠标)与点击灯箱(Esc / 点击关闭),均为 fixed 浮层,不占布局、不扰动同步几何。

### 变更
- 全部视图统一图片限高 `60vh`:超出按比例缩小,悬停放大或点击灯箱查看原图。
- 图片显示高度估算升级:显示高 = min(自然高 × 压缩比, 480px),并对复用缓存的块跳过重复探测。

### 修复
- 图片密集文档在分屏下的滚动同步失效:根因是图片段落高度按 HTML 字节数估算(约 43px vs 实际数千 px)、`<img>` 无宽高属性导致挂载实测高度为 0,且图片异步加载后无几何回填。现在渲染期读取本地图片真实尺寸并注入 `width`/`height`,浏览器加载前即按比例占位。
- 分屏预览中挂载块异步长高(外链图、尺寸探测失败、mermaid SVG)导致的滚动错位:`ResizeObserver` 自愈并重新锚定。

[完整变更](https://github.com/cangyunye/ruakdown/compare/v0.5.0...v0.5.1)

## [0.5.0] - 2026-09-16

相比 v0.4.0:

### 新增
- **代码块语法高亮**(syntect):覆盖预览、分屏预览、阅读视图、分享网页与 HTML 导出。
- 亮/暗两套高亮色板,经 `data-scheme` 随应用主题切换,10 款内置主题无需各自配置。
- 分块大文档路径共用同一高亮拦截,分块拼接结果与整篇渲染逐字节一致。

### 性能
- 块级 memo 缓存:输入防抖重渲染时未编辑代码块近乎零成本(40KB / 256 块热渲染约 0.4ms)。
- `SyntaxSet` 懒加载缓存,冷启动约 23ms。

### 说明
- mermaid 与未知语言代码块保持纯文本;输出 HTML 转义。

[完整变更](https://github.com/cangyunye/ruakdown/compare/v0.4.0...v0.5.0)

## [0.4.0] - 2026-09-16

相比 v0.3.3:

### 新增
- **分屏模式**:源码编辑与实时预览并排。模式三态「阅读 | 分屏 | 源码」,`Ctrl+Tab` 循环切换,原生菜单可直达。
- 分屏交互:分隔条拖拽调比例、`⇄` 互换左右、双击复位 50/50;方向与比例持久化到 `config.json`。
- 分屏联动:编辑与预览块级双向滚动同步(以编辑器为准,150ms 同步锁防回环),侧栏大纲取自预览元数据、点击跳转编辑器对应行、目录搜索命中定位行号、当前章节高亮由编辑器顶行驱动、外部修改提示覆盖分屏。

### 变更
- 预览统一走分块虚拟化管线(占位高度 + 按需挂载 + DOM 驱逐),不分文档大小一条路径,>1MB 文档同样流畅;meta 重建后按编辑器视口顶块重新对齐,上方改动不再跳动。

### 修复
- 进入分屏后预览不渲染(模式守卫读到切换前的旧值);预览构建失败从静默吞掉改为 error 横幅提示。

### 性能
- 预览文本哈希短路,并按(标题计数, 块源码)哈希复用未变块的 HTML;280ms 防抖 + 修订号 latest-wins,过期响应丢弃。

### ⚠️ 升级提示
- `Ctrl+Tab` 语义变更:v0.3.2 是在「阅读 / 源码」两视图间切换,现在是在「阅读 / 分屏 / 源码」三态间循环。

[完整变更](https://github.com/cangyunye/ruakdown/compare/v0.3.3...v0.4.0)

## [0.3.3] - 2026-09-16

相比 v0.3.2:

### 新增
- Markdown 链接跳转:项目内/外 `.md` 均在应用内打开且侧栏树保持不变,外链走系统浏览器,`#锚点`平滑滚动;源码模式支持 `Ctrl/Cmd+点击`。
- 双击 `.md` 文件直接打开被双击的文件(解析启动参数、单实例转发、macOS Opened 事件),优先级高于「恢复上次文件」。
- 测试基础设施:新增 Rust 单测 14 个(链接解析 / 启动参数 / 主题完整性)与前端 vitest 测试 23 个(链接路由 / 文件变更决策)。

### 修复
- 保存后反复误报「文件已被外部修改」:自身写入设 3 秒抑制窗口,不再弹窗也不再回读正文。

### 变更
- 提示横幅帘幕化:`error`/`info`/外部修改提示统一移入 fixed 覆盖层,正文布局零位移;全屏提示改为自动淡出的横幅。
- 论道武当主题重制为黑白织染风格(宣纸白底 + 墨黑文字与强调 + 墨块反白代码区)。

### ⚠️ 升级提示
- 抑制窗口的代价:本应用自身写入后的 3 秒内不再做外部变更检查,若他人恰好在这 3 秒内改了同一文件,该次改动不会立即提示(继续编辑或重新打开后仍会提示)。
- 「论道武当」主题外观与 v0.3.2 明显不同(重制为黑白织染)。

[完整变更](https://github.com/cangyunye/ruakdown/compare/v0.3.2...v0.3.3)

## [0.3.2] - 2026-09-15

相比 v0.3.1:

### 新增
- 全屏模式:`F11`(Windows/Linux)、`Ctrl+Cmd+F`(macOS);全屏时隐藏原生菜单栏。
- `Ctrl+Tab` 在两个视图间切换(阅读 / 源码)。

### 修复
- Windows 上 `Ctrl+Shift+F`(目录搜索)与 `Ctrl+Shift+Z`(专注模式)快捷键不触发:增加快捷键兜底处理。

[完整变更](https://github.com/cangyunye/ruakdown/compare/v0.3.1...v0.3.2)

## [0.3.1] - 2026-09-15

相比 v0.3.0:

### 新增
- 3 款内置配色主题:青梅煮酒、高山流水、论道武当(内置主题达 10 款)。

### 说明
- 本版仅新增主题,无其他功能或修复变更。

[完整变更](https://github.com/cangyunye/ruakdown/compare/v0.3.0...v0.3.1)

## [0.3.0] - 2026-09-15

相比 v0.2.0:

### 新增
- **专注模式 (Zen)**:阅读区按标题级别分节聚焦,当前章节保持清晰、其余调暗(可选调暗 + 模糊);分节级别自动检测(取出现 ≥2 次的最上层标题),可在设置中指定 H1/H2/H3;焦点跟随滚动切换;`←/→` 或 `j/k` 跳转章节,`Esc` 退出;状态栏显示当前节位。
- 重点增强(仅专注模式生效):粗体微放大 + 荧光笔色带、行内代码光晕、引用块 callout 化。
- `==高亮==` 语法支持(DOM 后处理,跳过代码块),所有模式可用。
- 4 款内置配色主题:夕阳海岸、无边绿意、蓝天白云、陈旧报纸(内置主题达 7 款)。
- 自动发布流水线:推送 `v*` tag 即构建并发布 Windows amd64 与 macOS arm64 安装包。

### 说明
- 专注模式入口:工具栏「专注」按钮、视图菜单、`Ctrl+Shift+Z`;分节级别与效果强度等设置持久化。分块渲染的大文档暂不支持专注模式。
- 本版是**首个提供 macOS 安装包**的版本(v0.2.0 只做了跨平台打包配置,未发布安装包)。

[完整变更](https://github.com/cangyunye/ruakdown/compare/v0.2.0...v0.3.0)

## [0.2.0] - 2026-09-15

相比 v0.1.0:

### 新增
- **目录内容搜索**(`Ctrl+Shift+F`):Rust 侧并发扫描 + 结果列表,点击跳转并定位到对应位置。
- **阅读区背景图**:自选本地图片,提供「纸面实色」与「半透明毛玻璃」两种样式,模糊度与蒙版浓度可调,蒙版颜色随主题自动适配,配置持久化。

### 变更
- 打包 `targets` 改为 `all`,新增 macOS/Linux/Windows 平台配置,具备跨平台打包能力。

### 说明
- 本版未发布安装包(仓库有 tag,Releases 页无此版本),首个跨平台安装包随 v0.3.0 提供。

[完整变更](https://github.com/cangyunye/ruakdown/compare/v0.1.0...v0.2.0)

## [0.1.0] - 2026-09-12

首个可用版本(Windows x64)。

### 阅读
- 文件夹树(文件变更自动刷新)、渲染视图、大纲导航(跳转 + 滚动同步高亮)
- Mermaid 12 图表(按需渲染 + 主题同步)
- 浅色 / 暗色 / 石墨三主题(窗口标题栏跟随)
- 重启恢复上次会话

### 编辑
- CodeMirror 6 源码模式、自动保存
- 逐文件保持编码(UTF-8 / BOM / UTF-16 / GBK)与换行风格(CRLF / LF)
- 保存前滚动备份、外部修改检测

### 输出
- 离线 HTML 导出(内联主题 CSS + Mermaid,断网可打开)
- 内嵌预览服务(axum,支持本机 / 局域网共享)
- 原生菜单栏与快捷键

### 大文档
- 超过 1MB 自动进入分块渲染(视口预取 + 按需加载 + DOM 逐出),快速滚动流畅

### Windows 专项
- `.md` 文件关联、单实例运行、相对路径图片本地显示
